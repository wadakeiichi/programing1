# ============================================
# ミニレポート 1
# 氏名: 〇〇 〇〇         ← 自分の氏名に書き換える
# 学籍番号: XXXXXXXX     ← 自分の学籍番号に書き換える
# 提出日: 2026-MM-DD
# ============================================
#
# 提出時のファイル名: report1_氏名.py に変更すること（例: report1_wada.py）
# 各セルは # %% で区切り、VS Code の「Run Cell」で 1 問ずつ実行できるようにすること。


# %% 問題1: 水素原子のボーア半径と基底状態エネルギー
# TODO: 下の定数を使って a0 と E1 を計算し、指定の単位で表示する。

hbar = 1.0546e-34   # 換算プランク定数 [J s]
m_e  = 9.109e-31    # 電子の質量 [kg]
e    = 1.602e-19    # 電気素量 [C]
k_e  = 8.988e9      # クーロン定数 [N m^2 / C^2]
eV   = 1.602e-19    # 1 eV を J に換算する係数 [J/eV]

# a0 = hbar^2 / (m_e * e^2 * k_e)  [m]
# a0 = ...   # TODO

# E1 = - m_e * e^4 * k_e^2 / (2 * hbar^2)  [J]
# E1 = ...   # TODO

# TODO: print(f"...") で
#   a0 を [m] と [Å]（1 Å = 1e-10 m）
#   E1 を [J] と [eV]
# の両方を表示する。書式は指数表記 4 桁または小数 4 桁。


# %% 問題2 (a): わざと壊れたコードを直す
# TODO: 下のコードをそのまま実行するとエラーが出る（少なくとも 3 箇所のルール違反がある）。
#       エラーメッセージを読んだうえで、直したコードをこのセルの下に書き直し、
#       何を直したかをコメントで明記すること。

# --- 壊れたコード（コピーして実行 → エラー確認 → 直す）---
# total = 0
# for i in range(1, 6)
#     total = total + i ** 2
#   print(f"途中経過: total = {total}")
# print(f'最終結果: 1^2 + 2^2 + ... + 5^2 = {total}')
# print(total = 55)

# --- 修正後のコード（ここに書く）---
# TODO


# %% 問題2 (b): 浮動小数点数の比較
# TODO: 下のコードを実行し、a == b が False になる理由を 3 行程度のコメントで自分の言葉で書く。

a = 0.1 + 0.2
b = 0.3
print(a == b)
print(f"a = {a:.20f}")
print(f"b = {b:.20f}")

# 考察（コメントで書く）:
#   ・出力結果は ...
#   ・なぜか？ → コンピュータの float は 2 進法で ...
#   ・正しい比較方法は math.isclose(a, b) または abs(a - b) < 1e-9 を使う。
